---
import { Image } from "astro:assets";
import bgCancha from "~/assets/bg_cancha.png";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Image
  class="h-full w-full object-cover"
  src={bgCancha}
  widths={widths}
  sizes={sizes}
  alt="Fair Play"
/>
